import UIKit

/// Define a concrete theme instance.
extension MyThemeModel {
    static let lightThemeModel = MyThemeModel(
        appBgColor: .white,
        highlightedBgColor: .lightGray,
        textColor: .black
    )
}
