import UIKit

/// Define a concrete theme instance.
extension MyThemeModel {
    static let darkThemeModel = MyThemeModel(
        appBgColor: .gray,
        highlightedBgColor: .darkGray,
        textColor: .white
    )
}
